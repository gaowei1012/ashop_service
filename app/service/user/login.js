'use strict'

const Service = require('egg').Service
const { SUCCESS, ERROR } = require('../../utils/util')

class LoginServie extends Service {
  async login() {
    const {ctx} = this
    try {
      // todo
      

    } catch(e) {
      // todo
    }
  }
}

module.exports = LoginServie
